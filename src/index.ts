import * as dotenv from 'dotenv';
import {
  classNamesToStyledComponents,
  oneComponentPerFile,
  sortedProps,
  tailwindClasses,
  unusedImports,
  variableCasing,
} from './nlp/favors';

dotenv.config();
// classNamesToStyledComponents();
// oneComponentPerFile();
// sortedProps();
// tailwindClasses();
// unusedImports();
// variableCasing();
